
@model IEnumerable<project_someren.Models.Room>
@{ ViewData["Title"] = "Rooms"; }

<div class="d-flex justify-content-between align-items-center mb-4 mt-3">
    <h1 class="display-5 fw-bold text-primary">Manage Rooms</h1>
    <a asp-action="Create" class="btn btn-lg btn-success shadow-sm rounded-pill px-4">
        <i class="bi bi-plus-circle me-2"></i>Create New
    </a>
</div>

<div class="card shadow-sm border-0 rounded-4 overflow-hidden mb-5">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover table-striped align-middle mb-0">
                <thead class="table-dark">
                    <tr>
                        <th>Building</th>
                <th>RoomNumber</th>
                <th>SquareMeters</th>
                <th>IsTeacherRoom</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in Model) {
                    <tr>
                        <td>@item.Building</td>
                    <td>@item.RoomNumber</td>
                    <td>@item.SquareMeters</td>
                    <td>@item.IsTeacherRoom</td>
                        <td class="text-end">
                            <div class="btn-group shadow-sm" role="group">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">Edit</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">Delete</a>
                            </div>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
</div>
